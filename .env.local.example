# Secret used to sign session cookies (HMAC). Optional for local/demo use —
# lib/session.ts falls back to a hardcoded demo secret if this is unset.
# Set a real random value (e.g. `openssl rand -base64 32`) for any deployment.
SESSION_SECRET=
